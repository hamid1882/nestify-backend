fastapi
uvicorn[standard]
gunicorn
databases[postgresql]
sqlalchemy
pydantic
asyncpg
psycopg2-binary

### **Step 2: Update Environment Variable**

Add this to your Render environment variables:
```
ENVIRONMENT=production
DATABASE_URL=<automatically filled when you select the database>
```